route: /about
title: About WorkAtlas
description: How WorkAtlas keeps configuration, content, application code, and live data separate.
published: true
sections:
  - type: hero
    eyebrow: Configuration-driven by design
    title: Flexible presentation with controlled application boundaries.
    description: YAML selects approved layouts and actions, Markdown holds editorial content, and PostgreSQL remains the source of truth for private workspace data.
    layout: centered
  - type: markdown
    source: about.md
  - type: buttonGroup
    alignment: center
    buttons:
      - label: Read the architecture guide
        action: readArchitecture
        variant: primary
      - label: Back home
        action: goHome
        variant: secondary
